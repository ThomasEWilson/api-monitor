import { LitElement, html, css } from 'lit-element';

export class CollectionParamsComponent extends LitElement {
  static get styles() {
    return css``;
  }

  // Implement `render` to define a template for your element.
  render() {
    return html``;
  }
}
